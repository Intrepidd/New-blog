<%= form_tag do%>
  <table>
    <tr>
      <td><%= label_tag(:username, "Login") %></td><td><%= text_field_tag :username, nil, {:class => "shadow"} %></td>
    </tr>
    <tr>
      <td><%= label_tag(:password, "Mot de passe") %></td><td><%= password_field_tag :password, nil, {:class => "shadow"} %></td>
    </tr>
    <tr>
      <td></td><td><%= submit_tag "Connexion" %></td>
    </tr>
  </table>
<% end %>

